I need you to create a complete Habitica Party Automation Bot for me. I have zero coding experience, so please create ALL files from scratch with detailed comments explaining what each part does.

PROJECT REQUIREMENTS:
- Platform: Node.js (for deployment on Render.com)
- Features needed:
  1. Automatic welcome message when someone joins the party
  2. Weekly ranking update (every Sunday) that updates the party description with member stats
  3. Auto-start quests if they've been pending for more than 24 hours

TECHNICAL SPECIFICATIONS:
- Runtime: Node.js (latest LTS version)
- Framework: Express.js (minimal web server for webhooks)
- Scheduler: node-cron (for weekly tasks)
- HTTP Client: axios (for Habitica API calls)
- Environment: .env file for secrets
- Deployment: Render.com free tier

FILE STRUCTURE NEEDED:
Create the following folder structure:

habitica-bot/
├── src/
│   ├── server.js           # Main Express server for webhooks
│   ├── scheduler.js        # Cron jobs for weekly tasks
│   ├── habiticaClient.js   # API wrapper for Habitica
│   ├── welcomeBot.js       # Logic for welcoming new members
│   ├── rankingBot.js       # Logic for weekly rankings
│   └── questStarter.js     # Logic for auto-starting quests
├── .env.example            # Template for environment variables
├── .gitignore              # Files to ignore in git
├── package.json            # Dependencies and scripts
├── README.md               # Setup instructions for me
└── render.yaml             # Render deployment config

DETAILED REQUIREMENTS FOR EACH FILE:

1. package.json:
   - Include dependencies: express, axios, dotenv, node-cron
   - Include start script: "node src/server.js"
   - Set engine to Node 18 or higher

2. src/habiticaClient.js:
   - Create a reusable class/module for making Habitica API calls
   - Include methods for:
     * GET /api/v3/groups/:groupId/members
     * GET /api/v3/groups/:groupId (to get party info)
     * POST /api/v3/groups/:groupId/chat (to send messages)
     * PUT /api/v3/groups/:groupId (to update description)
     * POST /api/v3/groups/:groupId/quests/force-start
   - Handle rate limiting (30 requests per 60 seconds)
   - Include proper error handling
   - Read API credentials from environment variables

3. src/welcomeBot.js:
   - Function that receives webhook data
   - Check if message is a "join" event (look for "has joined the party")
   - Extract the username from the message
   - Send a personalized welcome message to party chat
   - Include error handling and logging

4. src/rankingBot.js:
   - Function that fetches all party members
   - Calculate rankings based on total experience OR damage dealt
   - Sort members by the chosen metric
   - Generate a Markdown-formatted leaderboard table
   - Update the party description with the new leaderboard
   - Include top 10 members only

5. src/questStarter.js:
   - Function that checks if a quest is pending
   - Check how long it's been pending (compare timestamps)
   - If pending > 24 hours, force-start the quest
   - Send a message to chat notifying members
   - Include error handling

6. src/server.js (MAIN FILE):
   - Create Express server
   - Set up webhook endpoint: POST /webhook/chat
   - Call welcomeBot when webhook receives join message
   - Set up health check endpoint: GET /health (returns 200 OK)
   - Listen on PORT from environment variable (default 3000)
   - Include detailed console logging

7. src/scheduler.js:
   - Set up cron job to run rankingBot every Sunday at 9 AM
   - Set up cron job to run questStarter every 6 hours
   - Include logging for when jobs run

8. .env.example:
   - Include template for:
     * HABITICA_USER_ID=your-user-id-here
     * HABITICA_API_KEY=your-api-key-here
     * PARTY_ID=your-party-id-here
     * PORT=3000

9. .gitignore:
   - Ignore node_modules/
   - Ignore .env
   - Ignore .DS_Store

10. README.md:
    - Step-by-step setup instructions for a complete beginner
    - How to get Habitica API credentials
    - How to find Party ID
    - How to set up Render deployment
    - How to configure webhooks in Habitica
    - Troubleshooting section

11. render.yaml:
    - Configure as a Web Service
    - Set build command: npm install
    - Set start command: npm start
    - Include environment variable placeholders

IMPORTANT CODING STANDARDS:
- Add extensive comments explaining what each function does
- Use async/await for all API calls
- Include try/catch blocks for error handling
- Console.log every major action (for debugging)
- Use clear variable names
- Keep functions small and focused (single responsibility)

HABITICA API DETAILS YOU MUST USE:
- Base URL: https://habitica.com/api/v3
- Required headers for every request:
  * x-api-user: {user's UUID}
  * x-api-key: {user's API key}
  * x-client: {user-id}-AutomationBot
  * Content-Type: application/json (for POST/PUT)
- Rate limit: Max 30 requests per 60 seconds
- Webhook event to listen for: groupChatReceived

DEPLOYMENT INSTRUCTIONS TO INCLUDE IN README:
1. Create account on Render.com
2. Connect GitHub repository
3. Create new Web Service
4. Select the repository
5. Set environment variables in Render dashboard
6. Deploy
7. Copy the webhook URL from Render
8. Register webhook in Habitica API settings

Please create all these files now with complete, working code. Make sure everything is beginner-friendly with lots of explanatory comments.